# MCP Tool Catalog

> **Не редактировать руками.** Этот файл генерируется из живого кода
> скриптом ``scripts/generate_tool_catalog.py`` через
> ``build_catalog()`` (``blocksnet_agent/tools/catalog.py``).
> Тест ``tests/test_tool_catalog_docs.py`` гарантирует, что закоммиченная
> версия совпадает с актуальной.

MCP-server ``blocksnet_mcp`` экспонирует 39 инструментов (36 каталожных
+ 3 служебных: ``open_session``, ``close_session``, ``session_info``).

Из каталожных инструментов:

- 6 читают ``state[result_key]`` — вызывайте их **после** ``load_blocks``,
  ``compute_*``, и в рамках сессии (``session_id`` в каждом вызове).
- Остальные — идемпотентные (``list_*``, ``compute_*`` с дефолтами, ``load_*``) либо
  генераторы (``suggest_target_blocks``, ``render_metric_map``).

Все инструменты принимают первым аргументом ``session_id: str = "default"``.
``submit_answer`` (``TOOL_BLOCKLIST``) НЕ экспонируется — это терминальный
инструмент агентского цикла, см. ``blocksnet_agent/tools/catalog.py``.

---

## `build_adjacency_graph`

**Краткое описание:** Строит граф пространственной смежности городских кварталов.

**Справка:**

```
Строит граф пространственной смежности городских кварталов.
```

**Вход:**

- `buffer_size` (`integer`, необязательный) _(default: `0)`_

**ToolSpec:** `name=build_adjacency_graph`, `short='Строит граф пространственной смежности городских кварталов.'`

---

## `build_blocks_with_services`

**Краткое описание:** Собирает ``blocks_with_services.gpkg`` из сырья в ``DATA_DIR``.

**Справка:**

```
Собирает ``blocks_with_services.gpkg`` из сырья в ``DATA_DIR``.

        Берёт ``blocks_path`` (по умолчанию ``DATA_DIR/blocks.gpkg``) как базовые полигоны
        кварталов; если такого файла нет, пробует ``DATA_DIR/blocks_with_services.gpkg``
        как готовый артефакт (no-op копия). Опционально присоединяет: ``buildings_path``
        (population), ``functional_zones_path`` + ``land_use_rules_path`` (land_use),
        ``DATA_DIR/services/*.geojson`` (count_<slug>; пропускается, если колонки
        уже есть в исходнике). Каталог сервисов валидируется против ``service_type_path``.

        Когда вызывать: один раз перед ``load_blocks``, если в ``DATA_DIR`` ещё нет
        готового ``blocks_with_services.gpkg``. После сборки ``load_blocks`` подхватит
        файл автоматически.

        Не путать с: ``load_blocks`` — он только читает, не собирает.
```

**Вход:**

- `blocks_path` (`string`, необязательный) _(default: `"")`_
- `buildings_path` (`string`, необязательный) _(default: `"")`_
- `functional_zones_path` (`string`, необязательный) _(default: `"")`_
- `land_use_rules_path` (`string`, необязательный) _(default: `"")`_
- `out_path` (`string`, необязательный) _(default: `"blocks_with_services.gpkg")`_
- `service_type_path` (`string`, необязательный) _(default: `"")`_

**ToolSpec:** `name=build_blocks_with_services`, `short='Собирает ``blocks_with_services.gpkg`` из сырья в ``DATA_DIR``.'`

---

## `compute_area_accessibility`

**Краткое описание:** Вычисляет площадно-взвешенную доступность.

**Справка:**

```
Вычисляет площадно-взвешенную доступность.
```

**Вход:**

- `out` (`boolean`, необязательный) _(default: `true)`_

**ToolSpec:** `name=compute_area_accessibility`, `short='Вычисляет площадно-взвешенную доступность.'`

---

## `compute_connectivity`

**Краткое описание:** Вычисляет связность транспортной сети из сохранённого результата доступности (нормированная).

**Справка:**

```
Вычисляет связность транспортной сети из сохранённого результата доступности (нормированная).

        Требует предварительно вычисленную доступность (mean/median/max); если её нет, mean считается
        автоматически. accessibility_key — какой результат доступности использовать.
```

**Вход:**

- `accessibility_key` (`string`, необязательный) _(default: `"mean_accessibility")`_

**ToolSpec:** `name=compute_connectivity`, `short='Вычисляет связность транспортной сети из сохранённого результата доступности (нормированная).'`

---

## `compute_density_indicators`

**Краткое описание:** Вычисляет индикаторы плотности застройки (FSI/GSI/MXI/OSR) для каждого квартала.

**Справка:**

```
Вычисляет индикаторы плотности застройки (FSI/GSI/MXI/OSR) для каждого квартала.

        Для сравнения кварталов по интенсивности застройки и потенциалу преобразований. Зависят от
        полноты атрибутов модели; не заменяют нормативную проверку.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=compute_density_indicators`, `short='Вычисляет индикаторы плотности застройки (FSI/GSI/MXI/OSR) для каждого квартала.'`

---

## `compute_development_indicators`

**Краткое описание:** Вычисляет индикаторы освоенности территории (требует fsi/gsi/mxi из density-индикаторов).

**Справка:**

```
Вычисляет индикаторы освоенности территории (требует fsi/gsi/mxi из density-индикаторов).
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=compute_development_indicators`, `short='Вычисляет индикаторы освоенности территории (требует fsi/gsi/mxi из density-индикаторов).'`

---

## `compute_land_use_accessibility`

**Краткое описание:** Вычисляет доступность (мин) до кварталов определённого типа землепользования.

**Справка:**

```
Вычисляет доступность (мин) до кварталов определённого типа землепользования.

        land_use из enum LandUse: RESIDENTIAL, BUSINESS, RECREATION, TRANSPORT, INDUSTRIAL, SPECIAL.
        Среднее/медиана — общая близость к зоне; худшие кварталы выявляют пространственные разрывы.
```

**Вход:**

- `land_use` (`string`, **обязательный**)
- `out` (`boolean`, необязательный) _(default: `true)`_

**ToolSpec:** `name=compute_land_use_accessibility`, `short='Вычисляет доступность (мин) до кварталов определённого типа землепользования.'`

---

## `compute_max_accessibility`

**Краткое описание:** Вычисляет максимальное время доступности для каждого квартала.

**Справка:**

```
Вычисляет максимальное время доступности для каждого квартала.
```

**Вход:**

- `out` (`boolean`, необязательный) _(default: `true)`_

**ToolSpec:** `name=compute_max_accessibility`, `short='Вычисляет максимальное время доступности для каждого квартала.'`

---

## `compute_mean_accessibility`

**Краткое описание:** Вычисляет среднее время доступности (мин) для каждого квартала по матрице доступности.

**Справка:**

```
Вычисляет среднее время доступности (мин) для каждого квартала по матрице доступности.

        Меньшее время — лучше; высокий максимум указывает на периферийность/разрывность сети.
        out=True — исходящая доступность (как из квартала доступны другие), out=False — входящая
        (как сам квартал доступен из других). Это доступность до ВСЕХ кварталов, не до конкретного сервиса.
```

**Вход:**

- `out` (`boolean`, необязательный) _(default: `true)`_

**ToolSpec:** `name=compute_mean_accessibility`, `short='Вычисляет среднее время доступности (мин) для каждого квартала по матрице доступности.'`

---

## `compute_median_accessibility`

**Краткое описание:** Вычисляет медианное время доступности для каждого квартала.

**Справка:**

```
Вычисляет медианное время доступности для каждого квартала.
```

**Вход:**

- `out` (`boolean`, необязательный) _(default: `true)`_

**ToolSpec:** `name=compute_median_accessibility`, `short='Вычисляет медианное время доступности для каждого квартала.'`

---

## `compute_population_centrality`

**Краткое описание:** Вычисляет центральность кварталов на основе населения и графа смежности.

**Справка:**

```
Вычисляет центральность кварталов на основе населения и графа смежности.

        Граф смежности строится автоматически, если его нет в кэше. Это относительный индекс.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=compute_population_centrality`, `short='Вычисляет центральность кварталов на основе населения и графа смежности.'`

---

## `compute_road_congestion`

**Краткое описание:** Строит OD-матрицу и рассчитывает загруженность рёбер дорожного графа.

**Справка:**

```
Строит OD-матрицу и рассчитывает загруженность рёбер дорожного графа.

        Экспериментальная метрика. Реализация — вендоренная
        ``origin_destination_matrix`` + ``road_congestion`` из
        ``blocksnet_agent.vendor.road_congestion`` (upstream
        aimclub/blocksnet @ feat/road_congestion @ 3a2ea5f).

        Требует в ``data_dir``: ``blocks_with_services.gpkg``,
        ``blocks_to_nodes.pickle``, ``nodes_to_nodes.pickle``,
        ``graph_drive.graphml``. Альтернативы ``*.pkl`` и ``drive.graphml``
        поддержаны. Входные файлы готовятся до вызова инструмента.

        Граф: int EPSG в ``graph['crs']``, ``x``/``y`` узлов, ``time_min`` и
        ``lanes`` рёбер. ``lanes`` нормализуется как в upstream:
        ``lanes < 1`` → 1, значения > 8 отвергаются заранее с понятным
        сообщением (иначе ``KeyError`` в ``_get_capacity_by_lanes``).

        Кварталы: ``population``, ``land_use``, ``site_area``, ``count_*``;
        ``capacity_*`` автоматически преобразуются в ``count_*``.

        ``accessibility`` — порог block→node в минутах (ближайший узел
        включается всегда). ``max_trips`` — предохранитель: расчёт не
        запускается, если OD больше. ``od_top_pairs`` — сколько пар OD писать
        в CSV (топ по объёму, sparse-формат; полная матрица живёт только в
        ``state['origin_destination_matrix']``).

        Результаты: ``state['origin_destination_matrix']``,
        ``state['road_congestion_edges']``; CSV
        ``origin_destination_matrix.csv`` (топ-N пар) и
        ``road_congestion_edges.csv``. ``congestion_level > 1`` — допустимая
        перегрузка.

        Семантика OD: модель origin-constrained, поездки ≈ Σ население ×
        ``trip_rate(land_use)`` (RESIDENTIAL=1.0, BUSINESS=2.7, INDUSTRIAL=2.0,
        SPECIAL=1.2, TRANSPORT=1.0, RECREATION=1.4, AGRICULTURE=0.2).
        Назначение дискретное: ``nx.dijkstra`` на каждую поездку —
        ``O(поездки × Dijkstra)``. Поднятие ``max_trips`` выше реального
        числа поездок на территории ведёт к зависанию, а не к результату:
        вместо этого уменьшите территорию или агрегируйте узлы.
```

**Вход:**

- `accessibility` (`number`, необязательный) _(default: `10.0)`_
- `max_trips` (`integer`, необязательный) _(default: `50000)`_
- `od_top_pairs` (`integer`, необязательный) _(default: `200)`_

**ToolSpec:** `name=compute_road_congestion`, `short='Строит OD-матрицу и рассчитывает загруженность рёбер дорожного графа.'`

---

## `compute_scenario_provision`

**Краткое описание:** Независимо пересчитывает обеспеченность после добавления ёмкостей сценария по block_id (before→after).

**Справка:**

```
Независимо пересчитывает обеспеченность после добавления ёмкостей сценария по block_id (before→after).

        Главный инструмент проверки ЭФФЕКТА развития: даёт измеренные before→after по сервисам.
        scenario=None + use_cached_solution=True берёт последнее предложение propose_zone_development;
        либо задай scenario вручную ({block_id: {service: capacity}}). Для проверки целевого сервиса передай
        его имя в service_set (например 'school'), а не пресет; пресет пересчитывает весь набор.

        Когда выбирать: после любого предложения добавить capacity, чтобы проверить измеренный эффект.
        Не путать с: propose_zone_development — он генерирует сценарий, а этот инструмент проверяет before→after.

Индекс сервисов: русские/английские синонимы помогают выбрать валидный service_type. Примеры: pitch (спорт/спортив/спортплощад), sports_centre (спортцентр/спорткомплекс/фитнес), school (школ/school/education), kindergarten (детсад/детский сад/садик), polyclinic (поликлиник/клиник/медицин), pharmacy (аптек/лекарств/pharmacy), convenience (магазин/продукт/ритейл), bus_stop (останов/автобус/транспорт), swimming_pool (бассейн/swimming), extra_education (допобраз/круж/секци), cafe (кафе/coffee/cafe), park (парк/зелён/зелен).
```

**Вход:**

- `accessibility_minutes` (`integer`, необязательный) _(default: `15)`_
- `max_depth` (`integer`, необязательный) _(default: `1)`_
- `scenario` (`any`, необязательный)
- `service_set` (`string`, необязательный) _(default: `"basic")`_
- `use_cached_solution` (`boolean`, необязательный) _(default: `true)`_

**ToolSpec:** `name=compute_scenario_provision`, `short='Независимо пересчитывает обеспеченность после добавления ёмкостей сценария по block_id (before→after).'`

---

## `compute_service_provision`

**Краткое описание:** Вычисляет конкурентную обеспеченность населения сервисом (или набором key/basic/advanced/comfort).

**Справка:**

```
Вычисляет конкурентную обеспеченность населения сервисом (или набором key/basic/advanced/comfort).

        Параметры: service_type — конкретный сервис из list_service_types() ИЛИ пресет
        'key'/'basic'/'advanced'/'comfort' (тогда считается батч по набору, сохраняется только сводка);
        accessibility_minutes — порог доступности (по умолчанию берётся из норматива SERVICE_TYPES,
        обычно 15 для школы и 60 для hospital); max_depth — глубина конкуренции.
        Выход: strong/weak provision (доля удовлетворённого спроса: strong — консервативная оценка,
        weak — расширенная) и число кварталов с полной/частичной/нулевой обеспеченностью.
        Подводные камни: не подменяй обеспеченность на compute_services_count (количество ≠ покрытие);
        service_type должен существовать среди capacity_*; если в list_key_services/list_service_types
        service помечен provision_available=False, demand-норматива нет и обеспеченность надо трактовать
        осторожно: лучше использовать capacity напрямую или близкий нормируемый сервис.

        Когда выбирать: чтобы оценить покрытие населения сервисом или найти кварталы с дефицитом.
        Не путать с: compute_services_count — количество объектов не равно обеспеченности спроса.

Индекс сервисов: русские/английские синонимы помогают выбрать валидный service_type. Примеры: pitch (спорт/спортив/спортплощад), sports_centre (спортцентр/спорткомплекс/фитнес), school (школ/school/education), kindergarten (детсад/детский сад/садик), polyclinic (поликлиник/клиник/медицин), pharmacy (аптек/лекарств/pharmacy), convenience (магазин/продукт/ритейл), bus_stop (останов/автобус/транспорт), swimming_pool (бассейн/swimming), extra_education (допобраз/круж/секци), cafe (кафе/coffee/cafe), park (парк/зелён/зелен).
```

**Вход:**

- `accessibility_minutes` (`any`, необязательный)
- `max_depth` (`integer`, необязательный) _(default: `1)`_
- `service_type` (`any`, **обязательный**)

**ToolSpec:** `name=compute_service_provision`, `short='Вычисляет конкурентную обеспеченность населения сервисом (или набором key/basic/advanced/comfort).'`

---

## `compute_services_centrality`

**Краткое описание:** Вычисляет составной индекс центральности кварталов на основе сервисов и доступности.

**Справка:**

```
Вычисляет составной индекс центральности кварталов на основе сервисов и доступности.

        Композит из связности + разнообразия + плотности сервисов. Высокая центральность — узлы,
        где целесообразно концентрировать общественные функции и пересадки. Это ОТНОСИТЕЛЬНЫЙ индекс,
        не абсолютная мощность.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=compute_services_centrality`, `short='Вычисляет составной индекс центральности кварталов на основе сервисов и доступности.'`

---

## `compute_services_collocation`

**Краткое описание:** Анализирует совместное расположение типов сервисов в кварталах.

**Справка:**

```
Анализирует совместное расположение типов сервисов в кварталах.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=compute_services_collocation`, `short='Анализирует совместное расположение типов сервисов в кварталах.'`

---

## `compute_services_count`

**Краткое описание:** Подсчитывает количество объектов каждого типа сервиса по кварталам.

**Справка:**

```
Подсчитывает количество объектов каждого типа сервиса по кварталам.

        Это КОНТЕКСТ (сколько объектов стоит), а НЕ показатель покрытия населения. Для обеспеченности
        спроса используй compute_service_provision; количество ≠ обеспеченность.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=compute_services_count`, `short='Подсчитывает количество объектов каждого типа сервиса по кварталам.'`

---

## `compute_services_density`

**Краткое описание:** Вычисляет плотность сервисов для каждого квартала.

**Справка:**

```
Вычисляет плотность сервисов для каждого квартала.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=compute_services_density`, `short='Вычисляет плотность сервисов для каждого квартала.'`

---

## `compute_shannon_diversity`

**Краткое описание:** Вычисляет индекс разнообразия Шеннона для распределения сервисов по кварталам.

**Справка:**

```
Вычисляет индекс разнообразия Шеннона для распределения сервисов по кварталам.

        Высокие плотность и Shannon diversity указывают на многофункциональные узлы; низкие — на зоны
        дефицита/моно-функции. Количество сервисов ≠ обеспеченность населения.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=compute_shannon_diversity`, `short='Вычисляет индекс разнообразия Шеннона для распределения сервисов по кварталам.'`

---

## `compute_shared_provision`

**Краткое описание:** Вычисляет совместную обеспеченность населения сервисом в заданном пороге доступности.

**Справка:**

```
Вычисляет совместную обеспеченность населения сервисом в заданном пороге доступности.

        Распределение обычно сильно скошено (у многих кварталов 0): опирайся на медиану, перцентили
        и долю кварталов без обеспеченности, а не на среднее. service_type — конкретный сервис или 'key'.
```

**Вход:**

- `accessibility_minutes` (`integer`, необязательный) _(default: `15)`_
- `service_type` (`any`, **обязательный**)

**ToolSpec:** `name=compute_shared_provision`, `short='Вычисляет совместную обеспеченность населения сервисом в заданном пороге доступности.'`

---

## `find_tools`

**Краткое описание:** Находит подходящие инструменты BlocksNet по запросу (keyword-поиск по описаниям).

**Справка:**

```
Находит подходящие инструменты BlocksNet по запросу (keyword-поиск по описаниям).

        Используй, когда не уверен, какой инструмент нужен под задачу. Возвращает список
        наиболее релевантных инструментов с краткими описаниями. Затем посмотри полное
        описание выбранного через get_tool_help(name) перед вызовом.
```

**Вход:**

- `query` (`string`, **обязательный**)

**ToolSpec:** `name=find_tools`, `short='Находит подходящие инструменты BlocksNet по запросу (keyword-поиск по описаниям).'`

---

## `get_analysis_results` ⚠️ **требует сессии** (читает `state[result_key]`)

**Краткое описание:** Извлекает из кэша краткую сводку ранее вычисленного результата.

**Справка:**

```
Извлекает из кэша краткую сводку ранее вычисленного результата.
```

**Вход:**

- `result_key` (`string`, **обязательный**)

**ToolSpec:** `name=get_analysis_results`, `short='Извлекает из кэша краткую сводку ранее вычисленного результата.'`

---

## `get_block_info` ⚠️ **требует сессии** (читает `state[result_key]`)

**Краткое описание:** Возвращает атрибуты квартала И поквартальные значения всех уже вычисленных метрик из кэша.

**Справка:**

```
Возвращает атрибуты квартала И поквартальные значения всех уже вычисленных метрик из кэша.

        Когда выбирать: когда в задаче назван конкретный block_id или нужно заземлить вывод по кварталу.
        Не путать с: get_analysis_results — он показывает городскую сводку и первые строки, а не
        обязательную детализацию для одного block_id.
```

**Вход:**

- `block_id` (`integer`, **обязательный**)

**ToolSpec:** `name=get_block_info`, `short='Возвращает атрибуты квартала И поквартальные значения всех уже вычисленных метрик из кэша.'`

---

## `get_metric_for_block` ⚠️ **требует сессии** (читает `state[result_key]`)

**Краткое описание:** Возвращает поквартальное значение метрики result_key для block_id с честной позицией в распределении.

**Справка:**

```
Возвращает поквартальное значение метрики result_key для block_id с честной позицией в распределении.

        Параметр column: для широких DataFrame (с несколькими числовыми колонками) — имя конкретной
        колонки. Без column при неоднозначности возвращается список доступных колонок.

        Когда выбирать: после compute_* или батча, если нужно процитировать значение именно для block_id.
        Не путать с: агрегатами strong/weak по городу — они не описывают конкретный квартал.
```

**Вход:**

- `block_id` (`integer`, **обязательный**)
- `column` (`string`, необязательный) _(default: `"")`_
- `result_key` (`string`, **обязательный**)

**ToolSpec:** `name=get_metric_for_block`, `short='Возвращает поквартальное значение метрики result_key для block_id с честной позицией в распределении.'`

---

## `get_tool_help`

**Краткое описание:** Возвращает ПОЛНОЕ описание инструмента BlocksNet: параметры, контракт, интерпретацию, подводные камни.

**Справка:**

```
Возвращает ПОЛНОЕ описание инструмента BlocksNet: параметры, контракт, интерпретацию, подводные камни.

        Вызывай перед использованием незнакомого инструмента, чтобы не ошибиться в параметрах.
```

**Вход:**

- `name` (`string`, **обязательный**)

**ToolSpec:** `name=get_tool_help`, `short='Возвращает ПОЛНОЕ описание инструмента BlocksNet: параметры, контракт, интерпретацию, подводные камни.'`

---

## `get_weakest_services` ⚠️ **требует сессии** (читает `state[result_key]`)

**Краткое описание:** Возвращает K слабейших сервисов квартала по уже вычисленной competitive_provision_*.

**Справка:**

```
Возвращает K слабейших сервисов квартала по уже вычисленной competitive_provision_*.

        Сначала вычисли нужные сервисы через compute_service_provision(service_type или preset).
        Если кэша ещё нет, инструмент сам считает компактный preset 'key' без артефактов и затем
        ранжирует поквартальные значения для block_id с контекстом распределения по городу.

        Когда выбирать: когда вопрос спрашивает, чего не хватает в конкретном квартале.
        Не путать с: get_analysis_results по городскому батчу — он не даёт дефициты именно block_id.

Индекс сервисов: русские/английские синонимы помогают выбрать валидный service_type. Примеры: pitch (спорт/спортив/спортплощад), sports_centre (спортцентр/спорткомплекс/фитнес), school (школ/school/education), kindergarten (детсад/детский сад/садик), polyclinic (поликлиник/клиник/медицин), pharmacy (аптек/лекарств/pharmacy), convenience (магазин/продукт/ритейл), bus_stop (останов/автобус/транспорт), swimming_pool (бассейн/swimming), extra_education (допобраз/круж/секци), cafe (кафе/coffee/cafe), park (парк/зелён/зелен).
```

**Вход:**

- `block_id` (`integer`, **обязательный**)
- `k` (`integer`, необязательный) _(default: `5)`_

**ToolSpec:** `name=get_weakest_services`, `short='Возвращает K слабейших сервисов квартала по уже вычисленной competitive_provision_*.'`

---

## `list_cached_data` ⚠️ **требует сессии** (читает `state[result_key]`)

**Краткое описание:** Возвращает список всех наборов данных в кэше агента с их размерами.

**Справка:**

```
Возвращает список всех наборов данных в кэше агента с их размерами.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=list_cached_data`, `short='Возвращает список всех наборов данных в кэше агента с их размерами.'`

---

## `list_key_services`

**Краткое описание:** Возвращает доступные сервисы с нормативами demand и accessibility (service_type.json / blocksnet).

**Справка:**

```
Возвращает доступные сервисы с нормативами demand и accessibility (service_type.json / blocksnet).
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=list_key_services`, `short='Возвращает доступные сервисы с нормативами demand и accessibility (service_type.json / blocksnet).'`

---

## `list_service_types`

**Краткое описание:** Возвращает список типов сервисов из загруженных кварталов с флагом доступности provision.

**Справка:**

```
Возвращает список типов сервисов из загруженных кварталов с флагом доступности provision.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=list_service_types`, `short='Возвращает список типов сервисов из загруженных кварталов с флагом доступности provision.'`

---

## `load_accessibility_matrix`

**Краткое описание:** Загружает предвычисленную матрицу доступности из data/acc_mx.pickle.

**Справка:**

```
Загружает предвычисленную матрицу доступности из data/acc_mx.pickle.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=load_accessibility_matrix`, `short='Загружает предвычисленную матрицу доступности из data/acc_mx.pickle.'`

---

## `load_blocks`

**Краткое описание:** Загружает GeoDataFrame кварталов с сервисами из data/blocks_with_services.gpkg.

**Справка:**

```
Загружает GeoDataFrame кварталов с сервисами из data/blocks_with_services.gpkg.
```

**Вход:**

*(без аргументов)*

**ToolSpec:** `name=load_blocks`, `short='Загружает GeoDataFrame кварталов с сервисами из data/blocks_with_services.gpkg.'`

---

## `prepare_accessibility_matrix`

**Краткое описание:** Готовит ``acc_mx.pickle`` (матрица доступности block→block) через Overpass.

**Справка:**

```
Готовит ``acc_mx.pickle`` (матрица доступности block→block) через Overpass.

        Использует ``blocksnet.relations.accessibility``:
          - ``get_accessibility_graph(blocks, graph_type)`` — walk/drive/intermodal граф;
          - ``calculate_accessibility_matrix(blocks, graph)`` — pandas-матрица.

        Сохраняет в ``out_path`` (по умолчанию ``DATA_DIR/acc_mx.pickle``). ``load_accessibility_matrix``
        подхватит файл автоматически.

        Когда вызывать: один раз перед ``load_accessibility_matrix``, если pickle отсутствует
        или устарел. **Overpass-запрос занимает минуты**, используйте ``graph_type='walk'``
        для самого дешёвого варианта.

        Не путать с: ``load_accessibility_matrix`` — только читает; ``compute_*_accessibility`` —
        считает производные метрики (mean/median/max) из уже загруженной матрицы.
```

**Вход:**

- `dtype` (`string`, необязательный) _(default: `"float32")`_
- `graph_type` (`string`, необязательный) _(default: `"walk")`_
- `out_path` (`string`, необязательный) _(default: `"acc_mx.pickle")`_

**ToolSpec:** `name=prepare_accessibility_matrix`, `short='Готовит ``acc_mx.pickle`` (матрица доступности block→block) через Overpass.'`

---

## `prepare_road_congestion_inputs`

**Краткое описание:** Готовит три файла, нужные ``compute_road_congestion``:

**Справка:**

```
Готовит три файла, нужные ``compute_road_congestion``:
          - ``graph_drive.graphml`` — drive MultiDiGraph (Overpass, ``additional_edgedata``);
          - ``blocks_to_nodes.pickle`` — block → drive-узел (через walk-граф);
          - ``nodes_to_nodes.pickle`` — drive-узел × drive-узел (время, мин).

        Использует ``blocksnet.relations.get_accessibility_graph`` (drive/walk)
        и ``iduedu.get_adj_matrix_gdf_to_gdf`` для матриц. ``buffer_m>0`` —
        обрезает кварталы буфером вокруг центроида (полезно для очень больших
        датасетов, чтобы Overpass не таймаутил).

        Когда вызывать: один раз перед ``compute_road_congestion``, если файлов
        нет или они устарели. **Overpass-запрос занимает минуты.**

        Не путать с: ``compute_road_congestion`` — только считает, не готовит входы.
```

**Вход:**

- `additional_edgedata` (`string`, необязательный) _(default: `"lanes")`_
- `buffer_m` (`integer`, необязательный) _(default: `0)`_
- `out_dir` (`string`, необязательный) _(default: `"")`_

**ToolSpec:** `name=prepare_road_congestion_inputs`, `short='Готовит три файла, нужные ``compute_road_congestion``:'`

---

## `propose_zone_development`

**Краткое описание:** Запускает area-based TPE-оптимизацию набора сервисов зоны и возвращает гипотезу развития.

**Справка:**

```
Запускает area-based TPE-оптимизацию набора сервисов зоны и возвращает гипотезу развития.

        service_set: пресет ('basic'/'advanced'/'comfort'/'key') ИЛИ конкретный сервис ('school', 'pitch', …).
        Передай КОНКРЕТНЫЙ сервис, чтобы сфокусировать оптимизацию на нём; пресет оптимизирует весь набор
        зоны и добавит не только целевой сервис. Пример фокуса: service_set='school', затем проверь
        эффект через compute_scenario_provision(service_set='school'). RESULT должен цитировать block_id,
        service_type и предложенную capacity.

        Когда выбирать: когда нужен вариант развития/добавления сервисов в уже выбранных block_id.
        Не путать с: suggest_target_blocks — он только ищет кандидатов, но не генерирует решение.

Индекс сервисов: русские/английские синонимы помогают выбрать валидный service_type. Примеры: pitch (спорт/спортив/спортплощад), sports_centre (спортцентр/спорткомплекс/фитнес), school (школ/school/education), kindergarten (детсад/детский сад/садик), polyclinic (поликлиник/клиник/медицин), pharmacy (аптек/лекарств/pharmacy), convenience (магазин/продукт/ритейл), bus_stop (останов/автобус/транспорт), swimming_pool (бассейн/swimming), extra_education (допобраз/круж/секци), cafe (кафе/coffee/cafe), park (парк/зелён/зелен).
```

**Вход:**

- `block_ids` (`array`, **обязательный**)
- `max_prov_evals` (`integer`, необязательный) _(default: `100)`_
- `max_runs` (`integer`, необязательный) _(default: `1000)`_
- `max_time_sec` (`integer`, необязательный) _(default: `300)`_
- `service_set` (`string`, необязательный) _(default: `"basic")`_
- `target_land_use` (`string`, необязательный) _(default: `"RESIDENTIAL")`_

**ToolSpec:** `name=propose_zone_development`, `short='Запускает area-based TPE-оптимизацию набора сервисов зоны и возвращает гипотезу развития.'`

---

## `render_metric_map` ⚠️ **требует сессии** (читает `state[result_key]`)

**Краткое описание:** Рисует PNG-картограмму для сохранённого поквартального результата BlocksNet.

**Справка:**

```
Рисует PNG-картограмму для сохранённого поквартального результата BlocksNet.
```

**Вход:**

- `result_key` (`string`, **обязательный**)
- `title` (`string`, необязательный) _(default: `"")`_

**ToolSpec:** `name=render_metric_map`, `short='Рисует PNG-картограмму для сохранённого поквартального результата BlocksNet.'`

---

## `suggest_target_blocks`

**Краткое описание:** Подбирает кандидаты block_id для оптимизации развития из кэша метрик или атрибутов модели.

**Справка:**

```
Подбирает кандидаты block_id для оптимизации развития из кэша метрик или атрибутов модели.

        criterion='low_provision' требует ПРЕДВАРИТЕЛЬНО вычисленной обеспеченности по service_type
        (сначала вызови compute_service_provision(service_type)); иначе вернёт просьбу её вычислить.
        criterion='high_population' — кварталы по убыванию населения. land_use — фильтр по типу зоны.
        Если целевой квартал уже известен из задачи, передавай его напрямую в propose_zone_development;
        suggest_target_blocks нужен для поиска кандидатов, когда block_id неизвестны.

Индекс сервисов: русские/английские синонимы помогают выбрать валидный service_type. Примеры: pitch (спорт/спортив/спортплощад), sports_centre (спортцентр/спорткомплекс/фитнес), school (школ/school/education), kindergarten (детсад/детский сад/садик), polyclinic (поликлиник/клиник/медицин), pharmacy (аптек/лекарств/pharmacy), convenience (магазин/продукт/ритейл), bus_stop (останов/автобус/транспорт), swimming_pool (бассейн/swimming), extra_education (допобраз/круж/секци), cafe (кафе/coffee/cafe), park (парк/зелён/зелен).
```

**Вход:**

- `count` (`integer`, необязательный) _(default: `10)`_
- `criterion` (`string`, необязательный) _(default: `"low_provision")`_
- `include_zero_demand` (`boolean`, необязательный) _(default: `false)`_
- `land_use` (`string`, необязательный) _(default: `"")`_
- `service_type` (`string`, необязательный) _(default: `"")`_

**ToolSpec:** `name=suggest_target_blocks`, `short='Подбирает кандидаты block_id для оптимизации развития из кэша метрик или атрибутов модели.'`

---

## Служебные инструменты

Эти три инструмента экспонируются только на уровне MCP (не входят в
каталог агента):

- `open_session(session_id='default', scenario_id=None, project_id=None)` —
  создаёт/возвращает сессию. `scenario_id` валидируется
  whitelist'ом `[a-zA-Z0-9_-]{1,64}` (path-traversal защита).
- `close_session(session_id)` — закрывает сессию, освобождает state.
- `session_info(session_id='default')` — диагностика (без значений state).

Сценарий-контракт см. `docs/tool_contract.md` § «Сессии MCP».
